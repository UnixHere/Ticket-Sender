# 🎟 QR Ticket Sender & Verifier

A two-part system for sending personalised QR ticket emails and checking students in at the entrance.

The repo includes **two Ticket Sender variants** — pick whichever fits your needs:

| | `main.py` — HTML version | `main_svg.py` — SVG version |
|---|---|---|
| **Ticket design** | HTML email template | Figma-exported SVG → PDF attachment |
| **Calendar invite** | ✗ | ✓ (.ics attached) |
| **Extra dependencies** | None | Playwright + Chromium |
| **When to use** | Quick setup, design in HTML | Pixel-perfect Figma design |

Both versions use the same Excel file, the same `.env` config, and the same Ticket Verifier.

---

## Project Structure

```
├── main.py                      # Ticket Sender — HTML version
├── main_svg.py                  # Ticket Sender — SVG version
├── prepare_svg.py               # SVG template preparation from Figma export
├── generate_ids.py              # Helper — fills missing IDs in the Excel file
├── ticket_template.html         # HTML email template (edit to customise design)
├── ticket_template.svg          # Prepared SVG template (generated by prepare_svg.py)
├── ticket_template_example.svg  # Example SVG template
├── webapp.py                    # Ticket Verifier — Flask backend
├── templates/
│   └── index.html               # Ticket Verifier — mobile scanning UI
├── students_database.xlsx       # Student list (do not commit!)
├── .env                         # API keys and event info (do not commit!)
└── ticket_preview/              # SVG version preview outputs (generated)
    ├── ticket_Name.svg
    ├── ticket_Name.pdf
    ├── ticket_Name.ics
    └── _adjust_test.pdf
```

---

## Excel Structure

All tools share the same `students_database.xlsx` file:

| Column | Header | Description |
|--------|--------|-------------|
| A | Name | Student's full name |
| B | Class | Class (e.g. `4.A`) |
| C | ID | Unique 3-digit number — use `generate_ids.py` if missing |
| D | Email | Student's email address |
| E | Sent | Set to `1` by Ticket Sender after sending |
| F | Arrived | Set to `1` by Ticket Verifier when scanned |

Columns E and F are created automatically on first run if they don't exist.

If students don't have IDs yet, generate them:

```bash
python generate_ids.py
```

This fills every empty cell in column C with a unique random 3-digit number (100–999). Existing IDs are never overwritten or duplicated. Supports up to 900 students. You can also pass a custom filename:

```bash
python generate_ids.py other_file.xlsx
```

---

## Installation

**HTML version:**
```bash
pip install resend "qrcode[pil]" openpyxl pillow python-dotenv flask flask-cors
```

**SVG version** (includes everything above, plus):
```bash
pip install playwright
playwright install chromium
```

> `playwright install chromium` downloads ~150 MB. Only needs to be done once.

Or install everything at once with `uv`:
```bash
uv sync
```

---

## .env Configuration

Create a `.env` file in the project folder. Both sender variants read from the same file:

```env
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx
SENDER_EMAIL=tickets@yourdomain.com
SENDER_NAME=Organiser
EVENT_NAME=Graduation 2026
EVENT_DATE=30.5.2026
EVENT_TIME=18:00
EVENT_LOCATION=Main Hall, High School
EVENT_DURATION_MINUTES=120
```

**Accepted `EVENT_DATE` formats:** `DD.MM.YYYY`, `YYYY-MM-DD`, `DD/MM/YYYY`, or a long-form date like `Friday, 30 May 2026`.

`EVENT_DURATION_MINUTES` is optional (default: 120). Only used by the SVG version for the calendar invite duration.

> **Never commit `.env`** — it contains API keys. Make sure it's in `.gitignore`.

> Resend requires a verified sending domain. The free tier allows 3,000 emails/month.

---

## 📤 Ticket Sender — HTML Version (`main.py`)

The simpler variant. Sends each student a personalised HTML email with their QR code embedded inline. No extra dependencies, no PDF generation.

### Customising the ticket design

Edit `ticket_template.html`. It's a standard HTML file with these placeholders:

| Placeholder | Value |
|-------------|-------|
| `{name}` | Student's full name |
| `{class_}` | Student's class |
| `{id_}` | Student's ID number |
| `{qr_cid}` | QR code image (keep as-is) |
| `{EVENT_NAME}` | From `.env` |
| `{EVENT_DATE}` | From `.env` |
| `{EVENT_TIME}` | From `.env` |
| `{EVENT_LOCATION}` | From `.env` |
| `{SENDER_EMAIL}` | From `.env` |

### Preview

In `main.py`, keep `MODE = "preview"` (default) and run:

```bash
python main.py
```

Preview mode does **not** send any emails. Instead it saves each student's rendered ticket as an HTML file and their QR code as a PNG into a `qr_preview/` folder. Open the HTML files in a browser to check the design before sending.

### Send

When you're happy with the result, set `MODE = "real"` in `main.py` and run:

```bash
python main.py
```

### Resend to one student

```bash
python main.py resend 123
python main.py resend "Jane Smith"
python main.py resend jane@school.com
```

---

## 📤 Ticket Sender — SVG Version (`main_svg.py`)

The more advanced variant. Generates a pixel-perfect PDF ticket from a Figma-exported SVG, and sends it as an email attachment along with a `.ics` calendar invite.

### 1. Prepare the SVG template

The `prepare_svg.py` tool adjusts your Figma export so it works with the system.

```bash
python prepare_svg.py your_ticket.svg
```

The script will ask you:
1. Which text element is the **student name** → replaces it with `{NAME_PLACEHOLDER}`
2. Which is the **class** → replaces it with `{CLASS_PLACEHOLDER}`
3. Optional **X offset** for each placeholder (shift right/left in pixels)
4. Whether you want to add a **QR sidebar** on the right (recommended)

The result is saved as `ticket_template.svg`.

> **Important:** The Figma export must contain actual `<text>` elements. If you used "Outline text", the text will have been converted to paths and the script won't find it. Re-export without that option.

### 2. Preview

In `main_svg.py`, keep `MODE = "preview"` (default) and run:

```bash
python main_svg.py
```

A `ticket_preview/` folder is generated with files for each student:
- `ticket_Name.svg` — generated ticket
- `ticket_Name.pdf` — final PDF (this is what gets sent)
- `ticket_Name.ics` — calendar invite

### 3. PDF text offset calibration

Playwright renders the SVG through a real Chromium browser, which ensures faithful reproduction of the Figma design. Occasionally the print renderer shifts text slightly differently. To calibrate:

```bash
python main_svg.py adjustpdf
```

This opens `ticket_preview/_adjust_test.pdf` (first student only). Adjust in `main_svg.py` based on the result:

```python
TEXT_Y_OFFSET = -10  # negative = up, positive = down
```

Repeat until the text sits correctly.

### 4. Send

When you're happy with the result, set `MODE = "real"` in `main_svg.py` and run:

```bash
python main_svg.py
```

Each email contains:
- **HTML body** with event information
- **PDF attachment** — personalised ticket with QR code
- **ICS attachment** — calendar invite (Google Calendar, Apple Calendar, Outlook)

### 5. Resend to one student

```bash
python main_svg.py resend 001
python main_svg.py resend "Jane Smith"
python main_svg.py resend jane@school.com
```

---

## Sending — shared behaviour

Both variants behave the same way when sending:

- Only students without `1` in column E are included — already-sent students are skipped
- You'll be asked to type `YES` to confirm before anything is sent
- After each successful send, column E is updated to `1`
- A 1.5 second delay is added between emails to avoid rate limits
- Students with no email address or no name are skipped with a warning
- Searching by ID, name, or email is case-insensitive; resending does **not** update column E

---

## 📷 Ticket Verifier (`webapp.py`)

A mobile-friendly web app you run on a laptop at the entrance. Any phone on the same WiFi can open it and use it as a QR scanner.

### Features

- 📷 **QR Scanner** — uses the phone camera to scan tickets
- ✅ **Instant verification** — checks the scanned QR against the Excel database
- ⚠️ **Duplicate detection** — warns if a ticket has already been scanned
- ✋ **Check-in button** — marks student as arrived in column F
- ↩ **Undo** — un-check a student if checked in by mistake
- 🔍 **Manual lookup** — search by name, ID, class, or email
- 📋 **Attendees list** — live stats and filters
- 📱 **QR code for the app URL** — prints a scannable QR in the terminal on startup so phones don't have to type the address

### Running

```bash
python webapp.py
```

The terminal will show the local IP address and a scannable QR code:

```
=======================================================
  🎟  Ticket Verifier
=======================================================
  ✓  Database: students_database.xlsx

  Open in browser (same WiFi):
  → http://192.168.1.42:5000

  Scan this QR code to open on any device:
  ...
  (Or visit http://192.168.1.42:5000/qr to download the QR as an image)
=======================================================
```

Open the address on a phone connected to the **same WiFi**.

> iPhone: use Safari for camera access  
> Android: use Chrome for camera access

### API Endpoints

| Method | Endpoint | Description |
|--------|----------|-------------|
| `POST` | `/api/verify` | Verify a QR string. Body: `{ "qr": "Name \| Class \| ID" }` |
| `POST` | `/api/mark_arrived` | Check in or undo. Body: `{ "row": 5, "arrived": true }` |
| `GET` | `/api/stats` | Returns `{ total, arrived, pending }` |
| `GET` | `/api/attendees` | Returns the full student list |
| `GET` | `/qr` | PNG QR code pointing to the app URL |

---

## QR Code Format

All tools use the same format:

```
Name | Class | ID
```

For example: `Jane Smith | 4.A | 142`

The verifier matches on **ID** (exact) and does a soft name check — a mismatch shows a warning but does not reject the ticket.
